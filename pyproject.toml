[project]
name = "jev-training"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "modal-training-gym",
]

[tool.uv.sources]
modal-training-gym = { git = "https://github.com/modal-projects/training-gym.git", branch = "main" }